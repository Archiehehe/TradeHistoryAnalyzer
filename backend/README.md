# TradeHistoryAnalyzer Backend

The backend is the FastAPI service for TradeHistoryAnalyzer. See the repository root `README.md` for setup instructions, environment variables, migrations, and testing.
